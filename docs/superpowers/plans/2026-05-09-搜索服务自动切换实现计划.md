# 联网搜索自动切换机制实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Tavily/Serper 自动切换，当一个服务不可用时切换到另一个

**Architecture:**
- 主服务: Tavily (专为 RAG 优化)
- 备选服务: Serper (Google 搜索)
- 自动切换: 检测到额度用完或错误时切换

---

## Task 1: 添加 Serper 备选服务

**Files:**
- Modify: `ai-qa-service/app/services/search.py`

- [ ] **Step 1: 修改 search.py**

```python
# ai-qa-service/app/services/search.py

import os
import httpx

# Tavily 配置
TAVILY_API_KEY = os.getenv("TAVILY_API_KEY", "")
TAVILY_API_URL = "https://api.tavily.com/search"

# Serper 配置
SERPER_API_KEY = os.getenv("SERPER_API_KEY", "")
SERPER_API_URL = "https://google.serper.dev/search"

# 当前使用的服务
current_service = "tavily"  # tavily 或 serper

class SearchServiceSwitcher:
    """搜索服务自动切换器"""
    
    def __init__(self):
        self.tavily_available = True
        self.serper_available = True
        self.current = "tavily"  # 默认使用 tavily
    
    def switch_to(self, service: str):
        """切换到指定服务"""
        self.current = service
        print(f"[搜索服务] 切换到: {service}")
    
    def mark_unavailable(self, service: str):
        """标记服务不可用"""
        if service == "tavily":
            self.tavily_available = False
            if self.serper_available:
                self.switch_to("serper")
        elif service == "serper":
            self.serper_available = False
    
    def reset(self):
        """重置所有服务状态"""
        self.tavily_available = True
        self.serper_available = True
        self.current = "tavily"

# 全局实例
switcher = SearchServiceSwitcher()

async def search_internet(query: str, depth: str = "basic") -> dict:
    """联网搜索，自动切换服务

    Args:
        query: 搜索问题
        depth: "basic" 或 "advanced"

    Returns:
        {
            "results": [...],
            "source": "tavily" | "serper",
            "error": str (如果有错误)
        }
    """
    global switcher
    
    # 尝试当前服务，失败则切换
    if switcher.current == "tavily" and switcher.tavily_available:
        result = await _search_tavily(query, depth)
        if result.get("error") == "quota_exceeded":
            switcher.mark_unavailable("tavily")
            # 尝试 serper
            if switcher.serper_available:
                result = await _search_serper(query)
            else:
                return {"results": [], "error": "Both Tavily and Serper are unavailable"}
        return result
    elif switcher.current == "serper" and switcher.serper_available:
        result = await _search_serper(query)
        if result.get("error") == "quota_exceeded":
            switcher.mark_unavailable("serper")
            if switcher.tavily_available:
                result = await _search_tavily(query, depth)
            else:
                return {"results": [], "error": "Both Tavily and Serper are unavailable"}
        return result
    else:
        # 两个都不可用，尝试任意可用的
        if switcher.tavily_available:
            return await _search_tavily(query, depth)
        elif switcher.serper_available:
            return await _search_serper(query)
        else:
            return {"results": [], "error": "All search services unavailable"}

async def _search_tavily(query: str, depth: str = "basic") -> dict:
    """Tavily 搜索"""
    if not TAVILY_API_KEY:
        return {"results": [], "error": "TAVILY_API_KEY not configured"}

    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            response = await client.post(
                TAVILY_API_URL,
                json={
                    "api_key": TAVILY_API_KEY,
                    "query": query,
                    "search_depth": depth,
                    "max_results": 5,
                    "include_answer": True,
                    "include_raw_content": False,
                    "include_images": False
                },
                headers={"Content-Type": "application/json"}
            )
            
            result = response.json()
            
            # 检查额度
            if response.status_code == 429:
                return {"results": [], "error": "quota_exceeded", "source": "tavily"}
            
            return {
                "results": [
                    {
                        "title": item.get("title", ""),
                        "url": item.get("url", ""),
                        "content": item.get("content", ""),
                        "source": "web"
                    }
                    for item in result.get("results", [])
                ],
                "answer": result.get("answer"),
                "source": "tavily"
            }
    except Exception as e:
        error_msg = str(e)
        if "429" in error_msg or "quota" in error_msg.lower():
            return {"results": [], "error": "quota_exceeded", "source": "tavily"}
        return {"results": [], "error": error_msg, "source": "tavily"}

async def _search_serper(query: str) -> dict:
    """Serper 搜索"""
    if not SERPER_API_KEY:
        return {"results": [], "error": "SERPER_API_KEY not configured"}

    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            response = await client.post(
                SERPER_API_URL,
                json={"q": query, "num": 5},
                headers={
                    "Content-Type": "application/json",
                    "X-API-Key": SERPER_API_KEY
                }
            )
            
            result = response.json()
            
            # 检查额度
            if response.status_code == 429:
                return {"results": [], "error": "quota_exceeded", "source": "serper"}
            
            # 解析 Serper 响应格式
            organic_results = result.get("organic", [])
            
            return {
                "results": [
                    {
                        "title": item.get("title", ""),
                        "url": item.get("link", ""),
                        "content": item.get("snippet", ""),
                        "source": "web"
                    }
                    for item in organic_results
                ],
                "source": "serper"
            }
    except Exception as e:
        error_msg = str(e)
        if "429" in error_msg or "quota" in error_msg.lower():
            return {"results": [], "error": "quota_exceeded", "source": "serper"}
        return {"results": [], "error": error_msg, "source": "serper"}

def get_current_service() -> str:
    """获取当前使用的搜索服务"""
    return switcher.current
```

- [ ] **Step 2: 更新 .env.example**

```
# AI 问答服务配置

# 硅基流动 API
SILICON_FLOW_API_KEY=your-silicon-flow-api-key

# 搜索服务 API (Tavily 和 Serper 会自动切换)
# Tavily: https://tavily.com (1000次/月)
TAVILY_API_KEY=your-tavily-api-key

# Serper: https://serper.dev (2500次/月)
SERPER_API_KEY=your-serper-api-key
```

- [ ] **Step 3: 测试**

```bash
# 测试搜索服务切换
curl -X POST "http://localhost:5002/api/chat/stream" \
  -H "Content-Type: application/json" \
  -d '{"question":"玉米价格","use_internet":true}'
```

---

## 验证方案

```bash
# 1. 设置两个 API Key
export TAVILY_API_KEY="your-tavily-key"
export SERPER_API_KEY="your-serper-key"

# 2. 启动后端
cd ai-qa-service && python -m app.main

# 3. 测试
curl -X POST "http://localhost:5002/api/chat/stream" \
  -H "Content-Type: application/json" \
  -d '{"question":"玉米价格","use_internet":true}'

# 4. 观察日志，确认使用的是哪个服务
```