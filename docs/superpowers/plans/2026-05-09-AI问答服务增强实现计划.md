# AI 问答服务增强实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 AI 问答服务流式响应、图表自动渲染、多源引用展示

**Architecture:**
- 后端使用 FastAPI SSE 流式响应，LLM 生成时嵌入 chart 标记
- 前端解析 SSE 流，使用 ECharts 渲染图表，支持文档预览和来源跳转

**Tech Stack:** Python FastAPI, Vue 3, ECharts 5, SSE, pdf.js

---

## 文件结构

```
ai-qa-service/
├── app/
│   ├── services/
│   │   ├── llm.py          # 流式生成 + chart 标记
│   │   └── vector.py       # 向量搜索
│   ├── api/
│   │   └── chat.py         # SSE 流式接口
frontend/src/
├── views/ai-chat/
│   ├── AiChat.vue              # 主页面改造
│   └── components/
│       ├── MessageContent.vue  # 消息解析组件
│       ├── ChartRenderer.vue   # 图表组件
│       ├── DocumentPreview.vue # 文档预览
│       └── SourceLink.vue      # 来源链接
└── api/
    └── index.ts           # API 类型定义
```

---

## Task 1: 后端流式生成支持

**Files:**
- Modify: `ai-qa-service/app/services/llm.py`
- Modify: `ai-qa-service/app/api/chat.py`

- [ ] **Step 1: 修改 llm.py 添加流式生成函数**

在 `app/services/llm.py` 中添加 `generate_answer_stream` 异步生成器函数，使用 `httpx.AsyncClient` 的 `stream` 模式调用硅基流动 API，yield 返回 `{"type": "text", "content": char}` 或 `{"type": "error", "content": str(e)}`

- [ ] **Step 2: 修改 chat.py 添加 SSE 流式接口**

在 `app/api/chat.py` 中添加 `POST /api/chat/stream` 接口，使用 `StreamingResponse` 返回 SSE 格式数据流

- [ ] **Step 3: 测试流式接口**

Run: `curl -X POST "http://localhost:5002/api/chat/stream" -H "Content-Type: application/json" -d '{"question":"玉米价格趋势"}'`

- [ ] **Step 4: Commit**

```bash
git add ai-qa-service/app/services/llm.py ai-qa-service/app/api/chat.py
git commit -m "feat(ai-qa): add streaming response support for chat API"
```

---

## Task 2: 前端 SSE 流式渲染

**Files:**
- Modify: `frontend/src/views/ai-chat/AiChat.vue`
- Modify: `frontend/src/api/index.ts`

- [ ] **Step 1: 修改 api/index.ts 添加流式接口类型**

添加 `ChatStreamRequest`、`ChatStreamResponse`、`Source` 类型定义，以及 `chatStream` 方法

- [ ] **Step 2: 修改 AiChat.vue 支持 SSE**

使用 `fetch` + `ReadableStream` 接收 SSE 流式数据，累积显示文本

- [ ] **Step 3: 添加打字机光标效果**

- [ ] **Step 4: 测试 SSE 流式接收**

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/ai-chat/AiChat.vue frontend/src/api/index.ts
git commit -m "feat(frontend): add SSE streaming support for AI chat"
```

---

## Task 3: 图表渲染组件

**Files:**
- Create: `frontend/src/views/ai-chat/components/ChartRenderer.vue`
- Create: `frontend/src/views/ai-chat/components/MessageContent.vue`
- Create: `frontend/src/views/ai-chat/components/SourceLink.vue`
- Modify: `frontend/src/views/ai-chat/AiChat.vue`
- Modify: `frontend/package.json` (安装 echarts)

- [ ] **Step 1: 创建 ChartRenderer.vue**

ECharts 图表组件，支持 line/bar/pie 三种类型，深色主题

- [ ] **Step 2: 创建 MessageContent.vue**

解析 `<chart>` 标记，渲染文字和图表

- [ ] **Step 3: 创建 SourceLink.vue**

来源链接组件，区分网页/文档/数据库图标

- [ ] **Step 4: 修改 AiChat.vue 集成组件**

- [ ] **Step 5: 安装 echarts 依赖**

Run: `cd frontend && npm install echarts`

- [ ] **Step 6: 测试图表渲染**

- [ ] **Step 7: Commit**

```bash
git add frontend/src/views/ai-chat/components/
npm install echarts
git add frontend/package.json frontend/src/views/ai-chat/AiChat.vue
git commit -m "feat(frontend): add chart rendering and source link components"
```

---

## Task 4: 文档预览组件

**Files:**
- Create: `frontend/src/views/ai-chat/components/DocumentPreview.vue`
- Modify: `frontend/src/views/ai-chat/AiChat.vue`

- [ ] **Step 1: 创建 DocumentPreview.vue**

支持 PDF/Word/图片预览的弹窗组件

- [ ] **Step 2: 修改 AiChat.vue 添加文档预览**

- [ ] **Step 3: 测试文档预览**

- [ ] **Step 4: Commit**

```bash
git add frontend/src/views/ai-chat/components/DocumentPreview.vue frontend/src/views/ai-chat/AiChat.vue
git commit -m "feat(frontend): add document preview component"
```

---

## Task 5: 联调测试

- [ ] **Step 1: 后端接口测试**

- [ ] **Step 2: 前端完整流程测试**

- [ ] **Step 3: 端到端测试**

- [ ] **Step 4: Commit**

---

## 验证命令

```bash
# 1. 启动后端
cd ai-qa-service && python -m app.main

# 2. 启动前端
cd frontend && npm run dev

# 3. 测试流式接口
curl -X POST "http://localhost:5002/api/chat/stream" \
  -H "Content-Type: application/json" \
  -d '{"question":"玉米价格趋势"}'

# 4. 访问前端
open http://localhost:3000/ai-chat
```