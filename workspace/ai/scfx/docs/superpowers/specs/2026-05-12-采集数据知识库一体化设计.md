# 采集数据 → 知识库 → 向量化 一体化设计

## 1. 概述

### 1.1 设计目标

将粮信网采集的数据无缝写入知识库，并自动触发向量化处理，实现：
- 采集流程统一管理
- 数据双写（原始数据 + 知识库）
- 分类映射自动化
- 向量化异步处理

### 1.2 整体流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   采集任务  │────▶│  Python SDK │────▶│  Java API   │────▶│   知识库    │
│   触发执行  │     │  采集粮信网  │     │  接收数据   │     │ t_knowledge │
└─────────────┘     └─────────────┘     └─────────────┘     └──────┬──────┘
                      │                                          │
                      │            ┌────────────────────────────┘
                      ▼            ▼
              ┌──────────────┐  ┌─────────────┐
              │   Report     │  │ KnowledgeBase│
              │  (原始数据)   │  │  (清洗后)    │
              └──────────────┘  └──────┬──────┘
                                        │
                        ┌───────────────┼───────────────┐
                        ▼               ▼               ▼
                 ┌───────────┐   ┌───────────┐   ┌───────────┐
                 │ 分类映射   │   │ 向量队列   │   │ 定时任务   │
                 │  规则引擎   │   │  异步处理   │   │  兜底处理   │
                 └─────┬─────┘   └─────┬─────┘   └─────┬─────┘
                       │               │               │
                       └───────────────┼───────────────┘
                                       ▼
                              ┌─────────────┐
                              │  向量数据库  │
                              └─────────────┘
```

---

## 2. 数据库设计

### 2.1 新增表

```sql
-- 采集数据 → 知识库分类 映射规则
CREATE TABLE t_category_mapping (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    source_type     VARCHAR(50) NOT NULL COMMENT '来源类型: liangxinwang/mysteel/chinagrain',
    variety         VARCHAR(50) COMMENT '品种: corn/wheat/rice/soybean',
    report_type     VARCHAR(50) COMMENT '报告类型: 日报/周报/月报/晨报',
    category_id     BIGINT NOT NULL COMMENT '目标分类ID',
    priority        INT DEFAULT 0 COMMENT '优先级，数字越大优先级越高',
    enabled         TINYINT DEFAULT 1 COMMENT '是否启用: 0-禁用 1-启用',
    description     VARCHAR(200) COMMENT '规则描述',
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 向量化处理日志
CREATE TABLE t_vectorization_log (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    knowledge_id    BIGINT NOT NULL COMMENT '知识库ID',
    category_id     BIGINT COMMENT '分类ID',
    status          VARCHAR(20) COMMENT 'pending/processing/success/failed',
    retry_count     INT DEFAULT 0 COMMENT '重试次数',
    error_message   TEXT COMMENT '错误信息',
    vector_id       VARCHAR(100) COMMENT '向量库返回的ID',
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 向量化任务表
CREATE TABLE t_vectorization_task (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    category_id     BIGINT NOT NULL COMMENT '待处理的分类ID',
    batch_size      INT DEFAULT 10 COMMENT '每批处理数量',
    status          VARCHAR(20) COMMENT 'pending/processing/completed',
    total_count     INT DEFAULT 0 COMMENT '总数量',
    processed_count INT DEFAULT 0 COMMENT '已处理数量',
    failed_count    INT DEFAULT 0 COMMENT '失败数量',
    trigger_type    VARCHAR(20) COMMENT '触发方式: auto/cron/manual',
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at     TIMESTAMP NULL
);

-- 采集器脚本注册表
CREATE TABLE t_collector_script (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    source          VARCHAR(50) NOT NULL UNIQUE COMMENT '数据源标识',
    name            VARCHAR(100) COMMENT '采集器名称',
    version         VARCHAR(20) COMMENT '当前版本',
    script_path     VARCHAR(200) COMMENT '脚本路径',
    enabled         TINYINT DEFAULT 1 COMMENT '是否启用',
    config_template JSON COMMENT '配置模板JSON',
    description     VARCHAR(500) COMMENT '说明',
    last_run_at     TIMESTAMP NULL COMMENT '最后执行时间',
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 采集器脚本版本历史
CREATE TABLE t_collector_script_version (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    script_id       BIGINT NOT NULL,
    version         VARCHAR(20) NOT NULL,
    script_content  TEXT COMMENT '脚本内容快照',
    changelog       VARCHAR(500) COMMENT '变更说明',
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (script_id) REFERENCES t_collector_script(id)
);
```

### 2.2 扩展现有表

```sql
-- t_knowledge_base 新增字段
ALTER TABLE t_knowledge_base ADD COLUMN category_id BIGINT COMMENT '所属分类ID';
ALTER TABLE t_knowledge_base ADD COLUMN collection_source VARCHAR(50) COMMENT '采集来源: liangxinwang';
ALTER TABLE t_knowledge_base ADD COLUMN collection_variety VARCHAR(50) COMMENT '采集品种: corn/wheat';
ALTER TABLE t_knowledge_base ADD COLUMN collection_report_type VARCHAR(50) COMMENT '报告类型: 日报/周报';

-- t_collection_task 新增字段（支持分类映射）
ALTER TABLE t_collection_task ADD COLUMN default_category_id BIGINT COMMENT '默认写入分类ID';
ALTER TABLE t_collection_task ADD COLUMN auto_vectorize TINYINT DEFAULT 1 COMMENT '自动向量化: 0-关闭 1-开启';
ALTER TABLE t_collection_task ADD COLUMN vectorize_threshold INT DEFAULT 5 COMMENT '向量化触发阈值';
```

---

## 3. 后端 API 设计

### 3.1 CollectorController 数据提交扩展

```java
// 位置: backend/src/main/java/com/scfx/controller/CollectorController.java

/**
 * 上报采集数据
 * POST /collector/exec/{executionId}/data
 */
@PostMapping("/{executionId}/data")
public Result<Void> submitData(
        @PathVariable String executionId,
        @RequestBody Map<String, Object> request) {

    // 1. 构建 Report（原始数据）- 保持原有逻辑
    Report report = new Report();
    report.setTitle((String) request.get("title"));
    report.setSource((String) request.get("source"));
    report.setOriginalUrl((String) request.get("url"));
    report.setVariety((String) request.get("variety"));
    report.setReportType((String) request.get("reportType"));
    report.setContent((String) request.get("content"));
    report.setExecutionId(executionId);

    // 2. 检查 Report 去重（根据URL）
    if (report.getOriginalUrl() != null && reportService.existsByUrl(report.getOriginalUrl())) {
        return Result.error("报告已存在");
    }
    reportService.saveReport(report);

    // 3. 构建 KnowledgeBase（知识库）
    KnowledgeBase kb = new KnowledgeBase();
    kb.setTitle(report.getTitle());
    kb.setSourceType(report.getSource());
    kb.setSourceName(getSourceName(report.getSource()));
    kb.setOriginalUrl(report.getOriginalUrl());
    kb.setContent(report.getContent());
    kb.setPublishTime(report.getPublishTime());
    kb.setExecutionId(executionId);
    kb.setCollectionSource(report.getSource());
    kb.setCollectionVariety(report.getVariety());
    kb.setCollectionReportType(report.getReportType());
    kb.setVectorStatus("pending");  // 初始状态：待向量化

    // 4. 分类映射
    Long categoryId = categoryMappingService.map(
        kb.getSourceType(),
        kb.getCollectionVariety(),
        kb.getCollectionReportType()
    );
    kb.setCategoryId(categoryId);

    // 5. 内容Hash去重
    String hash = DigestUtils.md5Hex(kb.getContent());
    if (knowledgeBaseService.existsByHash(hash)) {
        return Result.error("知识已存在");
    }
    kb.setContentHash(hash);

    // 6. 写入知识库
    knowledgeBaseService.save(kb);

    // 7. 触发分类向量化（异步）
    vectorTaskService.enqueue(categoryId, kb.getId());

    return Result.success(Map.of("knowledgeId", kb.getId()));
}
```

### 3.2 分类映射 API

```java
// 位置: backend/src/main/java/com/scfx/controller/CategoryMappingController.java

@RestController
@RequestMapping("/api/category-mapping")
@RequiredArgsConstructor
public class CategoryMappingController {

    private final CategoryMappingService categoryMappingService;

    // 获取所有映射规则
    @GetMapping("/list")
    public Result<List<CategoryMapping>> list() {
        return Result.success(categoryMappingService.list());
    }

    // 创建映射规则
    @PostMapping
    public Result<Void> create(@RequestBody CategoryMapping mapping) {
        categoryMappingService.save(mapping);
        return Result.success();
    }

    // 更新映射规则
    @PutMapping("/{id}")
    public Result<Void> update(@PathVariable Long id, @RequestBody CategoryMapping mapping) {
        mapping.setId(id);
        categoryMappingService.updateById(mapping);
        return Result.success();
    }

    // 删除映射规则
    @DeleteMapping("/{id}")
    public Result<Void> delete(@PathVariable Long id) {
        categoryMappingService.removeById(id);
        return Result.success();
    }

    // 测试映射预览
    @GetMapping("/preview")
    public Result<Category> preview(
            @RequestParam String source,
            @RequestParam(required = false) String variety,
            @RequestParam(required = false) String reportType) {
        Long categoryId = categoryMappingService.map(source, variety, reportType);
        Category category = categoryService.getById(categoryId);
        return Result.success(category);
    }
}
```

### 3.3 向量化管理 API

```java
// 位置: backend/src/main/java/com/scfx/controller/VectorizationController.java

@RestController
@RequestMapping("/api/vectorization")
@RequiredArgsConstructor
public class VectorizationController {

    // 获取向量化任务列表
    @GetMapping("/tasks")
    public Result<Page<VectorizationTask>> tasks(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size) {
        return Result.success(vectorTaskService.page(page, size));
    }

    // 获取待向量化数量统计
    @GetMapping("/stats")
    public Result<Map<String, Object>> stats() {
        return Result.success(vectorTaskService.getStats());
    }

    // 手动触发分类向量化
    @PostMapping("/trigger/{categoryId}")
    public Result<Void> trigger(@PathVariable Long categoryId) {
        vectorTaskService.triggerCategory(categoryId, "manual");
        return Result.success();
    }

    // 批量手动触发
    @PostMapping("/trigger/batch")
    public Result<Void> triggerBatch(@RequestBody List<Long> categoryIds) {
        for (Long categoryId : categoryIds) {
            vectorTaskService.triggerCategory(categoryId, "manual");
        }
        return Result.success();
    }

    // 重试失败任务
    @PostMapping("/retry/{knowledgeId}")
    public Result<Void> retry(@PathVariable Long knowledgeId) {
        vectorTaskService.retry(knowledgeId);
        return Result.success();
    }
}
```

### 3.4 采集脚本管理 API

```java
// 位置: backend/src/main/java/com/scfx/controller/CollectorScriptController.java

@RestController
@RequestMapping("/api/collector/scripts")
@RequiredArgsConstructor
public class CollectorScriptController {

    // 获取脚本内容
    @GetMapping("/{source}")
    public Result<Map<String, Object>> getScript(@PathVariable String source) {
        CollectorScript script = scriptService.getBySource(source);
        return Result.success(Map.of(
            "source", script.getSource(),
            "name", script.getName(),
            "version", script.getVersion(),
            "content", scriptService.getCurrentContent(source),
            "description", script.getDescription()
        ));
    }

    // 获取脚本内容（指定版本）
    @GetMapping("/{source}/versions/{version}")
    public Result<String> getScriptVersion(
            @PathVariable String source,
            @PathVariable String version) {
        String content = scriptService.getVersionContent(source, version);
        return Result.success(content);
    }

    // 获取版本历史
    @GetMapping("/{source}/versions")
    public Result<List<ScriptVersion>> getVersionHistory(@PathVariable String source) {
        return Result.success(scriptService.getVersionHistory(source));
    }

    // 保存新版本
    @PostMapping("/{source}")
    public Result<Void> saveScript(
            @PathVariable String source,
            @RequestBody Map<String, String> request) {
        scriptService.saveNewVersion(
            source,
            request.get("version"),
            request.get("content"),
            request.get("changelog")
        );
        return Result.success();
    }

    // 回滚到指定版本
    @PostMapping("/{source}/rollback")
    public Result<Void> rollback(
            @PathVariable String source,
            @RequestBody Map<String, String> request) {
        scriptService.rollback(source, request.get("version"));
        return Result.success();
    }

    // 对比两个版本
    @GetMapping("/{source}/diff")
    public Result<Map<String, String>> diff(
            @PathVariable String source,
            @RequestParam String from,
            @RequestParam String to) {
        return Result.success(Map.of("diff", scriptService.diff(source, from, to)));
    }

    // 列出所有脚本
    @GetMapping("/list")
    public Result<List<CollectorScript>> listScripts() {
        return Result.success(scriptService.list());
    }
}
```

---

## 4. 服务层设计

### 4.1 CategoryMappingService

```java
@Service
@RequiredArgsConstructor
public class CategoryMappingService {

    private final CategoryMappingMapper mappingMapper;
    private final CategoryMapper categoryMapper;

    // 默认分类ID（未分类）
    private static final Long DEFAULT_CATEGORY_ID = 1L;

    /**
     * 根据来源、品种、报告类型映射到分类ID
     * 匹配规则：完全匹配优先 > 部分匹配
     */
    public Long map(String sourceType, String variety, String reportType) {
        // 1. 优先精确匹配（三个字段都匹配）
        CategoryMapping exact = findMapping(sourceType, variety, reportType);
        if (exact != null) return exact.getCategoryId();

        // 2. 匹配来源+品种
        CategoryMapping sourceVariety = findMapping(sourceType, variety, null);
        if (sourceVariety != null) return sourceVariety.getCategoryId();

        // 3. 只匹配来源
        CategoryMapping sourceOnly = findMapping(sourceType, null, null);
        if (sourceOnly != null) return sourceOnly.getCategoryId();

        // 4. 匹配通配规则（variety=* 或 reportType=*）
        CategoryMapping wildcard = findWildcardMapping(sourceType);
        if (wildcard != null) return wildcard.getCategoryId();

        // 5. 返回默认分类
        return DEFAULT_CATEGORY_ID;
    }

    private CategoryMapping findMapping(String source, String variety, String reportType) {
        LambdaQueryWrapper<CategoryMapping> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(CategoryMapping::getSourceType, source)
               .eq(variety != null, CategoryMapping::getVariety, variety)
               .eq(reportType != null, CategoryMapping::getReportType, reportType)
               .eq(CategoryMapping::getEnabled, 1)
               .orderByDesc(CategoryMapping::getPriority)
               .last("LIMIT 1");
        return mappingMapper.selectOne(wrapper);
    }
}
```

### 4.2 VectorTaskService

```java
@Service
@RequiredArgsConstructor
public class VectorTaskService {

    private final VectorizationTaskMapper taskMapper;
    private final VectorizationLogMapper logMapper;
    private final KnowledgeBaseMapper knowledgeMapper;
    private final VectorClient vectorClient;  // 向量库客户端

    // 阈值：达到多少条待处理数据时自动触发
    private static final int AUTO_TRIGGER_THRESHOLD = 5;
    // 最大重试次数
    private static final int MAX_RETRIES = 3;

    /**
     * 加入向量化队列
     */
    @Transactional
    public void enqueue(Long categoryId, Long knowledgeId) {
        // 1. 更新知识库状态
        KnowledgeBase kb = knowledgeMapper.selectById(knowledgeId);
        kb.setVectorStatus("pending");
        knowledgeMapper.updateById(kb);

        // 2. 记录日志
        VectorizationLog log = new VectorizationLog();
        log.setKnowledgeId(knowledgeId);
        log.setCategoryId(categoryId);
        log.setStatus("pending");
        logMapper.insert(log);

        // 3. 检查是否达到自动触发阈值
        long pendingCount = countPendingInCategory(categoryId);
        if (pendingCount >= AUTO_TRIGGER_THRESHOLD) {
            triggerCategory(categoryId, "auto");
        }
    }

    /**
     * 触发分类向量化（异步）
     */
    @Async
    public void triggerCategory(Long categoryId, String triggerType) {
        // 1. 查询该分类下所有待处理数据
        List<KnowledgeBase> pendingList = knowledgeMapper.selectPending(categoryId);
        if (pendingList.isEmpty()) return;

        // 2. 创建任务记录
        VectorizationTask task = new VectorizationTask();
        task.setCategoryId(categoryId);
        task.setTotalCount(pendingList.size());
        task.setStatus("processing");
        task.setTriggerType(triggerType);
        taskMapper.insert(task);

        // 3. 批量处理
        for (KnowledgeBase kb : pendingList) {
            processVectorization(kb, task);
        }

        // 4. 更新任务状态
        task.setStatus("completed");
        task.setCompletedAt(LocalDateTime.now());
        taskMapper.updateById(task);
    }

    /**
     * 处理单条向量化（带重试）
     */
    private void processVectorization(KnowledgeBase kb, VectorizationTask task) {
        int retryCount = 0;

        while (retryCount <= MAX_RETRIES) {
            try {
                // 调用向量库API
                VectorResult result = vectorClient.embed(kb.getContent());

                // 更新成功状态
                kb.setVectorStatus("vectorized");
                kb.setVectorIds(result.getVectorId());
                knowledgeMapper.updateById(kb);

                // 更新日志
                updateLog(kb.getId(), "success", result.getVectorId());
                task.setProcessedCount(task.getProcessedCount() + 1);
                return;

            } catch (Exception e) {
                retryCount++;
                if (retryCount > MAX_RETRIES) {
                    // 最终失败
                    kb.setVectorStatus("failed");
                    knowledgeMapper.updateById(kb);
                    updateLog(kb.getId(), "failed", e.getMessage());
                    task.setFailedCount(task.getFailedCount() + 1);
                } else {
                    // 指数退避重试: 2s, 4s, 8s
                    try {
                        Thread.sleep((long) Math.pow(2, retryCount) * 1000);
                    } catch (InterruptedException ie) {
                        Thread.currentThread().interrupt();
                    }
                }
            }
        }
    }
}
```

---

## 5. 采集脚本管理框架

### 5.1 脚本目录结构

```
python-collector-sdk/
│
├── collectorsdk/
│   ├── __init__.py
│   ├── base.py                 # BaseCollector 基类
│   ├── executor.py              # 统一执行引擎
│   ├── registry.py             # 采集器注册中心
│   ├── config_center.py         # 配置中心客户端
│   ├── version_manager.py       # 版本管理
│   │
│   ├── plugins/                 # 采集器插件目录
│   │   ├── __init__.py          # 插件注册表
│   │   ├── base.py              # 插件基类
│   │   │
│   │   ├── liangxinwang/        # 粮信网采集器
│   │   │   ├── __init__.py      # 注册到全局注册表
│   │   │   ├── collector.py     # 采集器实现
│   │   │   └── config.yaml      # 数据源配置
│   │   │
│   │   ├── mysteel/             # 我的钢铁采集器
│   │   └── chinagrain/          # 中华粮网采集器
│   │
│   └── utils.py
│
├── configs/                     # 配置文件目录
│   ├── liangxinwang.yaml
│   ├── mysteel.yaml
│   └── chinagrain.yaml
│
└── versions/                    # 版本历史存储
    ├── liangxinwang/
    │   ├── v2.1.3.py
    │   ├── v2.1.2.py
    │   └── changelog.md
    └── mysteel/
```

### 5.2 插件基类设计

```python
# collectorsdk/plugins/base.py

class BaseCollectorPlugin(ABC):
    """采集器插件基类"""

    SOURCE: str = None           # 如 "liangxinwang"
    VERSION: str = "1.0.0"     # 当前版本
    DESCRIPTION: str = None      # 描述

    def __init__(self, config: ReporterConfig):
        self.config = config
        self.logger = logging.getLogger(f"collector.{self.SOURCE}")

    @abstractmethod
    def login(self) -> bool:
        """登录逻辑，子类实现"""
        pass

    @abstractmethod
    def collect(self) -> List[CollectItem]:
        """采集逻辑，子类实现"""
        pass

    def before_collect(self):
        """采集前准备（可选重写）"""
        self.logger.info(f"[{self.SOURCE}] 开始采集...")

    def after_collect(self, items: List[CollectItem]):
        """采集后处理（可选重写）"""
        self.logger.info(f"[{self.SOURCE}] 采集完成，共 {len(items)} 条数据")

    def cleanup(self):
        """清理资源（可选重写）"""
        pass
```

### 5.3 统一执行引擎

```python
# collectorsdk/executor.py

def main():
    parser = argparse.ArgumentParser(description='采集器执行引擎')
    subparsers = parser.add_subparsers(dest='command', help='子命令')

    # run 命令
    run_parser = subparsers.add_parser('run', help='执行采集')
    run_parser.add_argument('--source', required=True, help='数据源')
    run_parser.add_argument('--task-id', type=int, help='任务ID')

    # list 命令
    subparsers.add_parser('list', help='列出所有采集器')

    # status 命令
    status_parser = subparsers.add_parser('status', help='查看状态')
    status_parser.add_argument('--source', required=True, help='数据源')

    # rollback 命令
    rollback_parser = subparsers.add_parser('rollback', help='回滚版本')
    rollback_parser.add_argument('--source', required=True, help='数据源')
    rollback_parser.add_argument('--version', required=True, help='目标版本')

    args = parser.parse_args()
    executor = Executor()

    if args.command == 'run':
        executor.run(args.source, args.task_id)
    elif args.command == 'list':
        executor.list()
    elif args.command == 'status':
        executor.status(args.source)
    elif args.command == 'rollback':
        executor.rollback(args.source, args.version)
```

---

## 6. 前端页面设计

### 6.1 采集管理页面（复用扩展）

```
┌──────────────────────────────────────────────────────────────────┐
│  采集管理                                                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  [任务列表]  [脚本管理]  [向量化监控]                              │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ 任务名称 │ 数据源 │ 版本   │ 状态 │ 向量化进度 │ 操作        │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │ 粮信网晨报 │ 粮信网 │ v2.1.3 │ ●运行│ 25/28   │[执行][编辑]│  │
│  │ 粮信网周报 │ 粮信网 │ v2.1.3 │ ○停止│ 10/10   │[执行][编辑]│  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

**扩展列：**
- `scriptVersion`: 脚本版本号
- `vectorProgress`: 向量化进度 (已处理/总数)

### 6.2 编辑任务弹窗（扩展）

```
┌──────────────────────────────────────────────────────────────────┐
│  编辑采集任务                                                     │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  基本信息                                                         │
│  ├─────────────────────────────────────────────────────────────┐ │
│  │ 任务名称: [粮信网晨报采集]                                  │ │
│  │ 数据源:   [粮信网 ▼]                                       │ │
│  │ 品种:     [玉米 ▼]                                         │ │
│  │ 采集类型: [晨报 ▼]                                         │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  写入知识库配置                                                   │
│  ├─────────────────────────────────────────────────────────────┐ │
│  │ 映射规则:                                                   │ │
│  │ ┌──────────────────────────────────────────────────────┐  │ │
│  │ │ 来源: 粮信网 │ 品种: 玉米 │ 类型: 晨报                │  │ │
│  │ │        ↓ 映射到分类                                    │  │ │
│  │ │ 分类: [粮信网/玉米/日报 ▼]                             │  │ │
│  │ │ 优先级: [10 ▼]                                         │  │ │
│  │ └──────────────────────────────────────────────────────┘  │ │
│  │                                                           │ │
│  │ 高级配置:                                                   │ │
│  │ ├─ 自动向量化: [✓ 开启]  阈值: [5] 条                    │ │
│  │ └─ 失败重试:   [✓ 开启]  最大次数: [3] 次                │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│                              [取消]  [保存]                        │
└──────────────────────────────────────────────────────────────────┘
```

### 6.3 脚本管理 Tab

```
┌──────────────────────────────────────────────────────────────────┐
│  脚本管理                                                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  [+ 新建任务]  [导入脚本]                                         │
│                                                                   │
│  脚本列表                                                         │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ 粮信网采集器                                                │  │
│  │ 当前版本: v2.1.3  │ 状态: ● 运行中  │ 最后更新: 2024-05-12│  │
│  │ [查看代码] [版本历史] [回滚] [上传新版本]                   │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │ 我的钢铁采集器                                              │  │
│  │ 当前版本: v1.5.0  │ 状态: ○ 停止    │ 最后更新: 2024-05-10│  │
│  │ [查看代码] [版本历史] [回滚] [上传新版本]                   │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

### 6.4 在线编辑器

```
┌─────────────────────────────────────────────────────────────────────────┐
│  脚本编辑 - 粮信网采集器 v2.1.3                                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  [编辑] [版本对比]                                    [取消] [保存 v2.1.4]│
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 1 │ from collectorsdk import BaseCollectorPlugin                   │ │
│  │ 2 │ from collectorsdk import ReporterConfig, CollectItem          │ │
│  │ 3 │                                                               │ │
│  │ 4 │ class LiangxinwangCollector(BaseCollectorPlugin):             │ │
│  │ 5 │     SOURCE = "liangxinwang"                                  │ │
│  │ 6 │     VERSION = "2.1.3"                                        │ │
│  │ 7 │                                                               │ │
│  │ 8 │     def __init__(self, config, task_id, username, password,  │ │
│  │ 9 │                  varieties=None, report_types=None):          │ │
│  │...│                                                                │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  变更说明: [修复cookie过期问题                                   ]        │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  版本历史                                                               │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 版本      │ 日期          │ 变更说明               │ 操作          │ │
│  ├───────────────────────────────────────────────────────────────────┤ │
│  │ v2.1.4   │ 2024-05-12    │ 修复cookie过期问题    │ [查看][回滚] │ │
│  │ v2.1.3   │ 2024-05-10    │ 新增玉米周报采集       │ [查看][回滚] │ │
│  │ v2.1.2   │ 2024-05-08    │ 修复翻页逻辑           │ [查看][回滚] │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.5 向量化监控 Tab

```
┌──────────────────────────────────────────────────────────────────┐
│  向量化监控                                                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  统计概览                     [手动触发]  [刷新]                   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                │
│  │ 待处理   │ │ 处理中   │ │ 已完成   │ │ 失败    │                │
│  │   23    │ │    2    │ │  1,256  │ │   3    │                │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                │
│                                                                   │
│  按分类统计                                                         │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ 分类             │ 待处理 │ 处理中 │ 已完成 │ 失败 │ 操作   │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │ 粮信网/玉米/日报  │   5    │   1    │  523   │  0   │[触发] │  │
│  │ 粮信网/小麦/周报  │   3    │   0    │  234   │  1   │[触发] │  │
│  │ 未分类           │   7    │   0    │   43   │  0   │[触发] │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  最近失败记录                                                       │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ 时间           │ 知识ID │ 分类        │ 错误信息      │ 操作│  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │ 2024-05-12 08:30│  1023  │ 粮信网/玉米  │ 连接超时...  │[重试]│  │
│  │ 2024-05-12 07:15│   987  │ 我的钢铁/钢材 │ 编码错误...  │[重试]│  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 7. 安全考虑

### 7.1 脚本编辑安全

| 风险 | 防护措施 |
|------|---------|
| 执行恶意代码 | 沙箱保存 - 只存储不执行 |
| 语法错误破坏运行 | 预校验 - 保存前检查Python语法 |
| 误操作覆盖生产 | 版本化管理 - 每次保存生成新版本 |
| 未授权修改 | 权限控制 - 只有管理员可以编辑 |

```java
// 保存前预校验
public void saveNewVersion(String source, String version, String content, String changelog) {
    // 1. 语法检查
    if (!pythonSyntaxValidator.isValid(content)) {
        throw new IllegalArgumentException("Python语法错误，请检查");
    }

    // 2. 禁止危险操作
    if (content.contains("os.system") || content.contains("subprocess")) {
        throw new IllegalArgumentException("禁止使用系统调用命令");
    }

    // 3. 保存新版本
    scriptVersionMapper.insert(new ScriptVersion(source, version, content, changelog));

    // 4. 更新当前版本指针
    CollectorScript script = findBySource(source);
    script.setVersion(version);
    scriptMapper.updateById(script);
}
```

---

## 8. 实现优先级

### Phase 1: 核心数据流（优先）
1. CollectorController 数据提交扩展（双写）
2. CategoryMappingService 分类映射
3. KnowledgeBase 实体扩展字段
4. 基本分类映射管理 API

### Phase 2: 向量化处理
1. VectorTaskService 向量队列
2. 异步触发机制
3. 定时兜底任务
4. 向量化监控 API

### Phase 3: 脚本管理
1. 采集脚本 Plugin 框架
2. 统一执行引擎
3. 版本管理
4. 在线编辑器前端

### Phase 4: 前端集成
1. 采集管理页面 Tab 扩展
2. 分类映射配置 UI
3. 脚本管理 UI
4. 向量化监控 UI

---

## 9. 复用现有代码

| 模块 | 复用度 | 说明 |
|------|--------|------|
| Collection.vue 任务列表 | **80%** | 扩展列（版本、向量化进度） |
| 任务执行逻辑 | **100%** | 完全复用 |
| 分页组件 | **100%** | 完全复用 |
| 新建/编辑弹窗 | **70%** | 新增分类映射配置区 |
| 脚本管理 Tab | **新增** | 新增页面区域 |
| 向量化监控 Tab | **新增** | 新增页面区域 |

---

## 11. 分类功能增强（从重度用户角度优化）

### P0 优先级 - 必备功能

#### 11.1 向量化状态可见
**问题**: 用户无法快速了解哪些知识未向量化、哪些失败、整体向量化进度如何

**解决方案**:
- 在 CategoryTree 每个分类节点上显示向量化统计徽章
  - 示例: `粮信网/玉米/日报 [23] 🔴3` 表示共23条，3条失败
- 分类详情页显示向量化进度条
- 侧边栏增加"向量化异常"快捷入口，显示所有失败条目的分类

**数据模型扩展**:
```sql
-- 分类节点扩展统计字段（缓存表）
CREATE TABLE t_category_vector_stats (
    category_id     BIGINT PRIMARY KEY,
    total_count     INT DEFAULT 0,
    vectorized_count INT DEFAULT 0,
    failed_count    INT DEFAULT 0,
    pending_count   INT DEFAULT 0,
    last_updated    TIMESTAMP
);
```

**UI 设计**:
```
┌─ 知识分类 ──────────────────────────┐
│ 🔍 搜索分类...                       │
├─────────────────────────────────────┤
│ 📁 粮信网                            │
│   ├── 📁 玉米                        │
│   │   ├── 📁 日报 [128] 🔴3          │  ← 徽章显示异常
│   │   └── 📁 周报 [45]              │
│   └── 📁 小麦                        │
│       └── 📁 日报 [89]              │
├─────────────────────────────────────┤
│ ⚠️ 向量化异常 (5)                    │  ← 快捷入口
└─────────────────────────────────────┘
```

#### 11.2 分类映射依赖显示
**问题**: 映射规则多了后，不知道哪些规则依赖某个分类，删除前无法判断影响范围

**解决方案**:
- 在分类详情页显示"该分类被哪些映射规则引用"
- 删除分类前弹出警告，显示所有依赖的映射规则
- 映射规则列表增加"目标分类"列，可点击跳转到分类

**UI 设计**:
```
┌─ 映射规则管理 ────────────────────────────────────┐
│ 来源        │ 品种  │ 类型  │ 目标分类            │ 优先级 │ 操作 │
├──────────────────────────────────────────────────┤
│ 粮信网      │ 玉米  │ 日报  │ 粮信网/玉米/日报 [▶] │  10   │[编辑][删除]│
│ 粮信网      │ 小麦  │ 日报  │ 粮信网/小麦/日报 [▶] │   8   │[编辑][删除]│
└──────────────────────────────────────────────────┘
                              [▶] 点击展开依赖关系
```

**删除分类时的警告**:
```
┌─ 确认删除 ────────────────────────────────────┐
│  ⚠️ 删除分类前请确认以下依赖:                  │
│                                               │
│  📋 映射规则依赖:                              │
│  - 粮信网 → 玉米 → 日报 → 此分类               │
│  - 粮信网 → 玉米 → 周报 → 此分类               │
│                                               │
│  📋 知识库依赖:                                │
│  - 共 128 条知识属于此分类                      │
│                                               │
│  [取消]                          [确认删除]     │
└───────────────────────────────────────────────┘
```

#### 11.3 采集来源统计
**问题**: 用户想知道每个分类下知识的采集来源分布

**解决方案**:
- 在分类详情页显示来源分布饼图/柱状图
- 在 CategoryTree 节点显示主要来源标签
- 支持按来源筛选知识

**UI 设计**:
```
┌─ 分类详情: 粮信网/玉米/日报 ─────────────────┐
│                                              │
│ 来源分布           │  数量   │  占比          │
│ ├─ 粮信网          │  128   │  ████████ 76%  │
│ ├─ 我的钢铁网      │   35   │  ███ 21%       │
│ └─ 中华粮网        │    5   │  █ 3%          │
│                                              │
│ 近7天新增: 23 条                             │
│ [查看全部知识]                               │
└──────────────────────────────────────────────┘
```

### P1 优先级 - 提升效率

#### 11.4 分类右键菜单 + 向量化操作
**问题**: 当前向量化操作需要跳转到专门的 Tab，分类和向量化操作割裂

**解决方案**:
- CategoryTree 节点增加右键菜单
- 右键菜单包含：触发向量化、查看向量化状态、重试失败、查看依赖
- 无需切换 Tab，在分类树上直接操作

**UI 设计**:
```
┌─ 知识分类 ──────────────────────────┐
│ 📁 粮信网                     [右键]│
│   ├── 📁 玉米                [右键]│
│   │   └── 📁 日报            [右键]│
│   │       └── 🔴3            │  ← 向量化失败徽章
│   └── 📁 小麦                [右键]│
└─────────────────────────────────────┘

右键菜单:
┌─────────────────────────┐
│ 📊 查看向量化状态        │
│ ⚡ 触发向量化            │
│ 🔄 重试失败 (3)          │
│ 🔗 查看映射依赖          │
│ 📈 查看采集统计          │
├─────────────────────────┤
│ ➕ 添加子分类            │
│ ✏️ 重命名                │
│ 🗑️ 删除                  │
└─────────────────────────┘
```

**交互流程**:
1. 右键点击分类节点
2. 选择操作类型
3. 弹窗显示结果/确认

#### 11.5 未分类快捷入口
**问题**: 未归类知识散落各处，需要频繁筛选查找

**解决方案**:
- 在 CategoryTree 侧边栏顶部固定显示"未分类"入口，红色高亮显示数量
- 未分类知识默认显示在知识库列表最前面
- 支持批量选择后一键归类

**UI 设计**:
```
┌─ 知识分类 ──────────────────────────┐
│ 🔍 搜索分类...                       │
├─────────────────────────────────────┤
│ ⚠️ 未分类 (12)                       │  ← 快捷入口，红色高亮
├─────────────────────────────────────┤
│ 📁 粮信网                            │
│   ├── 📁 玉米                        │
│   └── 📁 小麦                        │
└─────────────────────────────────────┘
```

#### 11.6 批量操作 Undo 支持
**问题**: 批量移动/删除后，如果操作错了，不好回滚

**解决方案**:
- 批量操作后保留操作记录（30分钟内可撤销）
- 操作完成后显示"撤销"按钮
- 撤销是反向操作，不是恢复原状

**UI 设计**:
```
┌─ 操作完成 ──────────────────────────┐
│ ✅ 批量移动完成                      │
│                                      │
│ 已将 23 条知识从                     │
│ [粮信网/玉米/日报] 移动到            │
│ [粮信网/玉米/周报]                   │
│                                      │
│        [撤销]  [关闭]               │
└──────────────────────────────────────┘
```

**数据模型**:
```sql
-- 批量操作记录表
CREATE TABLE t_batch_operation_log (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    operation_type  VARCHAR(20) NOT NULL COMMENT 'move/delete',
    target_category BIGINT NOT NULL,
    source_category BIGINT COMMENT '移动时记录来源',
    knowledge_ids   JSON NOT NULL COMMENT '涉及的知识的 ID 列表',
    operator        VARCHAR(50) NOT NULL,
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    undone_at       TIMESTAMP NULL COMMENT '撤销时间，为空表示未撤销',
    undo_operation  VARCHAR(20) COMMENT 'undo: reverse move/delete'
);
```

### P2 优先级 - 锦上添花

#### 11.7 批量操作预览确认
**问题**: 批量移动、删除前无法预览影响范围，怕选错

**解决方案**:
- 批量选择后显示"预览"按钮
- 预览弹窗列出将被影响的知识列表（前10条 + 总数）
- 确认页面显示操作摘要："将移动 23 条知识从 X 到 Y"

**UI 设计**:
```
┌─ 批量移动预览 ──────────────────────┐
│ 📋 将移动以下知识:                    │
│                                      │
│ 1. 2024年玉米市场周报 - 粮信网       │
│ 2. 玉米价格走势分析 - 我的钢铁网     │
│ 3. 黑龙江玉米日报 - 中华粮网         │
│ ... 共 23 条                         │
│                                      │
│ 从: 粮信网/玉米/日报                 │
│ 到: 粮信网/玉米/周报                 │
│                                      │
│        [取消]  [确认移动]            │
└──────────────────────────────────────┘
```

#### 11.8 收藏夹增强命名
- 收藏夹支持重命名
- 收藏夹支持嵌套
- 显示收藏夹内的知识数量

#### 11.9 删除时选择目标分类
- 删除分类时可以选择将知识迁移到哪个分类
- 避免知识丢失

---

## 12. 待确认事项（已解决）

| 序号 | 问题 | 决策 |
|-----|------|------|
| 1 | 映射规则导入导出 | ❌ 不需要（选项 B）|
| 2 | 向量化失败告警 | ✅ 站内信 + Webhook（选项 D）|
| 3 | Undo 生效时间 | ✅ 30 分钟（选项 A）|
| 4 | Monaco Editor | ✅ 确认（选项 A）|
| 5 | Cron 可视化配置 | ✅ 需要（选项 A）|

---

## 13. 文档信息

- **创建日期**: 2026-05-12
- **最后更新**: 2026-05-13
- **状态**: 设计评审完成，准备进入 implementation planning
- **下次步骤**: 调用 writing-plans skill 创建实施计划

## 14. 讨论记录（完整）

### 数据库设计
| 序号 | 问题 | 决策 |
|-----|------|------|
| Q1 | 映射规则匹配策略 | 选项 A：优先级 + 精确优先 |
| Q2 | 优先级范围 | 选项 B：0-10 |
| Q2 | 默认兜底策略 | 选项 1：写入未分类 |
| Q3 | 映射规则扩展字段 | A+B+C（生效时间、审计、统计） |
| Q4 | 向量化日志扩展 | A+B+C（耗时、模型版本、内容摘要） |
| Q5 | 采集脚本扩展 | A+B+D（分类、执行策略、执行统计） |
| Q6 | 脚本版本扩展 | A+B+C（状态、审计、回滚标记） |
| Q7 | 向量化任务扩展 | A+B+C（优先级、错误样本、触发来源） |

### API 设计
| 序号 | 问题 | 决策 |
|-----|------|------|
| Q8 | 数据去重策略 | 保持现状（URL + ContentHash） |
| Q9 | 映射失败处理 | 选项 B：写入未分类 + 告警 |
| Q10 | 向量化触发方式 | 选项 B：积累 N 条后批量触发 |
| Q11 | 映射规则查询接口 | 全选 A+B+D |
| Q12 | 向量化任务接口 | 全选 A+B+C+D+E |
| Q13 | 采集脚本管理接口 | 全选 A+B+C+D+E+F+G |

### 服务层设计
| 序号 | 问题 | 决策 |
|-----|------|------|
| Q15 | 映射服务扩展 | A+B+C（缓存、预热、异步记录） |
| Q16 | 向量化服务扩展 | A+B+C（持久化、状态机、并发控制） |
| Q17 | 脚本服务扩展 | A+C+D（预校验、健康检查、限流） |

### 脚本管理框架
| 序号 | 问题 | 决策 |
|-----|------|------|
| Q18 | 脚本目录管理 | 选项 C：数据库存版本 + 文件存当前 |
| Q19 | 脚本结构 | SDK + 单文件插件 |
| Q20 | 认证模式 | 框架支持，业务方按需实现 |
| Q24 | 参数传递机制 | 方案 D：配置文件 + 环境变量覆盖 |
| Q26 | 超时控制 | 选项 C：全局 + 阶段超时 |
| Q27 | 并发控制 | 选项 D：全局 + 单数据源混合 |
| Q28 | 重试机制 | 指数退避，最多重试3次 |
| Q31 | 汇报机制 | 方案 C：批量上报 + 轮询 |
| Q32 | 前端交互 | 方案 C：抽屉 + Tab 结合 |

### 安全设计
| 序号 | 问题 | 决策 |
|-----|------|------|
| Q36 | 脚本执行安全 | 预校验 + 运行时限制 |
| Q37 | API 认证 | 执行 ID + 密钥 |
| Q38 | 数据安全 | 加密存储 + 环境变量 |
| Q39 | 权限控制 | 角色分离（Admin/Operator/Viewer） |
